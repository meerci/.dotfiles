[user]
	email = chenbao@baidu.com
	name = chenbao
[alias]
	st = status
	co = checkout
	br = branch
	bra = branch --all
	cm = commit
	cma = commit --amend
	cmm = commit -m
	logl = log --oneline --graph
  rss = restore --staged
  cp = cherry-pick
	pushr = !git push origin HEAD:refs/for/$(git name-rev --name-only HEAD)
[core]
	editor = nvim
[pull]
	rebase = true
[push]
	default = simple
[merge]
  tool = diffview
[mergetool]
  prompt = false
  keepBackup = false
